name: Daily Release

on:
  workflow_dispatch:
    inputs:
      release_date:
        description: "Specify release date (YYYY-MM-DD)"
        required: false
        type: string
      source_branch:
        description: "Branch to release from"
        required: false
        type: string
        default: "auto-update"
      refresh_data:
        description: "Refresh data before release (recommended)"
        required: false
        type: boolean
        default: true
  schedule:
    - cron: "30 0 * * *"

env:
  NODE_VERSION: "20"
  PNPM_VERSION: "10.11.0"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  create-release:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout main branch for workflows
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main

      - name: Checkout source branch for data
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.source_branch != '' && github.event.inputs.source_branch != 'main' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.event.inputs.source_branch }}
          path: source-data

      - name: Copy data from source branch
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.source_branch != '' && github.event.inputs.source_branch != 'main' }}
        run: |
          # Copy data directories from source branch if different from main
          if [ -d "source-data/table" ]; then cp -r source-data/table/ ./; fi
          if [ -d "source-data/asns" ]; then cp -r source-data/asns/ ./; fi
          if [ -d "source-data/tags" ]; then cp -r source-data/tags/ ./; fi
          rm -rf source-data

      - name: Checkout auto-update branch for data (scheduled)
        if: ${{ github.event_name == 'schedule' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: auto-update
          path: source-data

      - name: Copy data from auto-update branch (scheduled)
        if: ${{ github.event_name == 'schedule' }}
        run: |
          # Copy data directories from auto-update branch
          if [ -d "source-data/table" ]; then cp -r source-data/table/ ./; fi
          if [ -d "source-data/asns" ]; then cp -r source-data/asns/ ./; fi
          if [ -d "source-data/tags" ]; then cp -r source-data/tags/ ./; fi
          rm -rf source-data

      - name: Setup tooling
        uses: ./.github/actions/setup-runtime
        with:
          node-version: ${{ env.NODE_VERSION }}
          pnpm-version: ${{ env.PNPM_VERSION }}
          install-deps: "true"
          setup-go: "false"
          build-mmdbwriter: "false"

      # 仅 workflow_dispatch 且 refresh_data==true 时，运行数据更新脚本
      - name: Refresh data (manual trigger only)
        if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.refresh_data == 'true' || github.event.inputs.refresh_data == true) }}
        run: |
          echo "🔄 Running data update before release..."
          pnpm fetch:all
          echo "✅ Data refresh complete."

      - name: Configure git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

      - name: Set release date
        id: date
        run: |
          if [ -n "${{ github.event.inputs.release_date }}" ]; then
            RELEASE_DATE="${{ github.event.inputs.release_date }}"
          else
            RELEASE_DATE=$(date -u +"%Y-%m-%d")
          fi
          echo "RELEASE_DATE=${RELEASE_DATE}" >> $GITHUB_ENV
          echo "date=${RELEASE_DATE}" >> $GITHUB_OUTPUT

      - name: Generate release files
        run: |
          echo "🚀 Generating release files using Node.js compression..."
          ls -la table/ asns/ tags/ 2>/dev/null || true

          # Use Node.js script with NAPI xz compression
          ./scripts/compress-release-files.js

      - name: Calculate checksums
        run: |
          cd release-assets
          sha256sum * > checksums.sha256
          cd -

      - name: Upload release assets artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-files-${{ steps.date.outputs.date }}
          path: release-assets

      - name: Generate release notes
        run: |
          cat > release-notes.md << EOF
          # BGP.Tools OpenDB - ${RELEASE_DATE}

          📅 **Release date**: ${RELEASE_DATE}

          ## 📦 Data included

          ### Full-Table Archive
          - \`table-${RELEASE_DATE}.jsonl.gz\` - Global routing table data (gzip compressed)
          - \`table-${RELEASE_DATE}.jsonl.xz\` - Global routing table data (xz compressed, smaller file)

          ### ASN-Mapping Table
          - \`asns-${RELEASE_DATE}.csv.gz\` - ASN to name mapping (gzip compressed)
          - \`asns-${RELEASE_DATE}.csv.xz\` - ASN to name mapping (xz compressed, smaller file)

          ### ASN-Tags Data
          - \`tags-*-${RELEASE_DATE}.csv.gz\` - Various category tag data (gzip compressed)
          - \`tags-*-${RELEASE_DATE}.csv.xz\` - Various category tag data (xz compressed, smaller file)

          ### Checksums
          - \`checksums.sha256\` - SHA256 checksums for all files

          ## 📊 Data sources

          All data comes from [bgp.tools](https://bgp.tools) HTTP API, obtained at: $(date -u +"%Y-%m-%d %H:%M UTC")

          ## 🔗 Related links

          - [Project homepage](https://github.com/${{ github.repository }})
          - [BGP.Tools](https://bgp.tools)
          - [Auto-update branch](https://github.com/${{ github.repository }}/tree/auto-update)

          ---

          *This release is automatically generated by GitHub Actions*
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.date.outputs.date }}
          name: BGP.Tools OpenDB - ${{ steps.date.outputs.date }}
          body_path: release-notes.md
          files: release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Clean up
        run: |
          rm -rf release-assets release-notes.md table-${RELEASE_DATE}.jsonl* asns-${RELEASE_DATE}.csv* tags-*-${RELEASE_DATE}.csv*
